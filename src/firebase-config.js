// THIS IS A PLACEHOLDER CONFIGURATION
// For the app to work, you must replace this with your own
// Firebase project configuration.
// See the README.md for more details.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// This is a placeholder for the app ID used in the database path.
// In a real deployment, this would be provided by the environment.
export const appId = 'default-app-id';

// This is a placeholder for the initial auth token.
// In a real deployment, this would be provided by the environment.
export const initialAuthToken = null;
